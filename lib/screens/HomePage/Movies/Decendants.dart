import 'package:flutter/material.dart';

class DescendantsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Descendants")),
      body: Center(child: Text("Details about Descendants")),
    );
  }
}
