import 'package:flutter/material.dart';

class StrangerThingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Stranger Things")),
      body: Center(child: Text("Details about Stranger Things")),
    );
  }
}
