import 'package:flutter/material.dart';

class RunOnScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Run On")),
      body: Center(child: Text("Details about Run On")),
    );
  }
}
