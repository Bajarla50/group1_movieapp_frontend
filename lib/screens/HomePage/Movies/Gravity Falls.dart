import 'package:flutter/material.dart';

class GravityFallsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Gravity Falls")),
      body: Center(child: Text("Details about Gravity Falls")),
    );
  }
}
