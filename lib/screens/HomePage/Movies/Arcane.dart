import 'package:flutter/material.dart';

class ArcaneScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Arcane")),
      body: Center(child: Text("Details about Arcane")),
    );
  }
}
