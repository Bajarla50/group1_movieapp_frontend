import 'package:flutter/material.dart';

class Frozen2Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Frozen 2")),
      body: Center(child: Text("Details about Frozen 2")),
    );
  }
}
