import 'package:flutter/material.dart';

class StarVsForcesOfEvilScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Star vs. The Forces of Evil")),
      body: Center(child: Text("Details about Star vs. The Forces of Evil")),
    );
  }
}
