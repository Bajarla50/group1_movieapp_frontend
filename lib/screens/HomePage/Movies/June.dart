import 'package:flutter/material.dart';

class JuneScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("June")),
      body: Center(child: Text("Details about June")),
    );
  }
}
