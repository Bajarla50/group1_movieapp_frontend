import 'package:flutter/material.dart';

class LokiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Loki")),
      body: Center(child: Text("Details about Loki")),
    );
  }
}
